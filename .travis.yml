language: cpp

matrix:
  include:
  - os: linux
    dist: bionic
    compiler: gcc
    sudo: required
    script:
      - g++ --version
      - sh scripts/travis_build.sh

  - os: osx
    osx_image: xcode11
    compiler: clang
    script:
      - sh scripts/travis_build.sh