language: cpp

matrix:
  include:
  - os: linux
    dist: bionic
    compiler: gcc
    sudo: required
    script:
      - sh Install.sh

  - os: osx
    osx_image: xcode11
    compiler: clang
    script:
      - sh Install.sh

  - os: windows
    before_install:
      - choco install python -y
    before_script:
      - choco install pip -y
    script:
      - ./Install.bat

notifications:
  email: false
