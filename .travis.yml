language: cpp

matrix:
  include:
  - os: linux
    dist: bionic
    compiler: gcc
    sudo: required
    script:
      - sh Build.sh

  - os: osx
    osx_image: xcode11
    compiler: clang
    script:
      - sh Build.sh

  - os: windows
    script:
      - ./Build.bat

notifications:
  email: false
