//#include "Engine.h"
#include "Core.h"

int32 GuardedMain(StringView cmdLine)
{
	return 0;
	/*Engine engine;

	int32 error = engine.Init();
	if (error) return error;

	error = engine.Run();
	engine.Release();
	return error;*/
}
