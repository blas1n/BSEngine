# Per-axis scripting ops that R2 grandfathers. Deliberately empty.
#
# It held 45 lines from roadmap item 32 until item 42 deleted every one of
# them: nothing outside the JS binding block had ever called any of them, and
# item 41 gave every concept they covered a whole-vector accessor first.
#
# Empty means R2 now forbids per-axis ops outright rather than merely holding
# the count where it was. Adding a line here is a deliberate act: say why in a
# comment above it.
