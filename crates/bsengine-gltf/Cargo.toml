[package]
name = "bsengine-gltf"
description = "GLTF/GLB import — mesh, material, and texture extraction"
version.workspace = true
edition.workspace = true

[dependencies]
bsengine-core.workspace = true
bsengine-render.workspace = true
bsengine-rhi-wgpu.workspace = true
bevy_app.workspace = true
bevy_asset.workspace = true
bevy_ecs.workspace = true
bevy_reflect.workspace = true
bsengine-asset.workspace = true
glam.workspace = true
gltf.workspace = true
tracing.workspace = true

[dev-dependencies]
bsengine-app.workspace = true
pollster.workspace = true
# Tests only: `GpuMeshRegistry`/`GpuTextureRegistry` are constructed from a real
# headless `wgpu::Device`, because the plugin only builds them when a winit
# window exists. See `insert_headless_gpu_registries` in `plugin.rs`.
wgpu.workspace = true
