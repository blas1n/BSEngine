[package]
name = "bsengine-render"
description = "Scene rendering pipeline — PBR, shadows, lights, frustum culling"
version.workspace = true
edition.workspace = true

[dependencies]
bsengine-core.workspace = true
bsengine-ecs.workspace = true
bsengine-input.workspace = true
bsengine-rhi-wgpu.workspace = true
bsengine-window.workspace = true
bevy_app.workspace = true
bevy_asset.workspace = true
bevy_ecs.workspace = true
bevy_reflect.workspace = true
bsengine-asset.workspace = true
glam.workspace = true
tracing.workspace = true

[dev-dependencies]
bsengine-app.workspace = true
bsengine-rhi-wgpu.workspace = true
# The texture-cache tests need a real headless device, which means blocking on
# an async constructor the same way every other GPU-touching test here does.
pollster.workspace = true
